# 各接入工具分计划（按工具特点，非统一框架）

> 2026-08-11
> 原则: 每个工具的天花板不同 → 创新点不同 → 实施路径不同。
> 逐工具分析后各自立项，共用 fastsec 的 stealth/diff/3-gate 资产但组合方式不同。

---

## 工具 1: sqlmap（P0 — 注入利用之王）

**天花板**（区别于其他工具的核心特征）:
- 慢: 每参数全库 payload 盲测，布尔盲注逐字符二分，单目标可跑小时级
- 黑盒盲目: 不利用已发现的技术指纹（DBMS 类型/参数语义）
- 指纹明显: sqlmap 默认 UA 特征，WAF 秒识别
- 误报: 无基线对比确认

**创新点（针对性的，非通用）**:
| # | 创新 | 解决的天花板 |
|---|------|------------|
| S1 | **指纹引导方言**: 先 whatweb/nuclei 指纹 → 判 DBMS（MySQL/MSSQL/PG/SQLite）→ 只注入对应 payload 集 | 黑盒盲目 → 请求量 -80% |
| S2 | **diff 预筛注入点**: fastsec diff 找行为差异参数 → 只送可疑参数给 sqlmap | 全参数盲测 → 聚焦 |
| S3 | **3-gate 确认**: sqlmap 报注入 → 基线对比真伪（能 UNION 出数据才算真） | 误报 → 零误报 |
| S4 | **stealth 前置**: UA 池 + 节奏 + 代理 → sqlmap | WAF 识别 → 难识别 |
| S5 | **结果入库**: 确认注入 → findings_store（candidate→verify） | 跨会话复用 |

**实施形态**: Go 编排器（oracle.py 指纹→方言 / prescreen.py diff 预筛 / stealth 封装 / confirm.py 3-gate）+ 调 sqlmap CLI
**验收**: 对比原生 sqlmap 同目标请求量 -60%+、误报归零、结果入库

---

## 工具 2: 爆破类（hydra/kerbrute/medusa/ncrack — P1）

**天花板**:
- 盲目字典: 用通用字典，不利用社工信息（真实姓名/域名/公司）
- 无节奏: 单账号连续爆破 → 触发锁定策略
- 无入库: 命中凭据丢失

**创新点**:
| # | 创新 | 解决 |
|---|------|------|
| C1 | **社工字典生成器**: OSINT（真实姓名/域名）→ 定制字典（姓名+年份/公司名+@+数字/拼音+生日） | 盲目 → 命中率 -10x 字典量 |
| C2 | **stealth 节奏**: 多账号轮换 + 随机间隔 + 每账号限 1-2 次（Kerberos 锁定 5 次/30min） | 锁账号 → 不触发 |
| C3 | **结果入库**: 命中 → findings_store（高价值凭据） | 丢失 → 复用 |

---

## 工具 3: 扫描类（nmap/rustscan/naabu/masscan — P2）

**天花板**: 静态端口-服务映射，不联动后续攻击
**创新点**:
| # | 创新 |
|---|------|
| N1 | **自动编排**: 扫描结果 → 服务指纹 → 自动选后续工具（web→fastsec / 内核版本→searchsploit / 数据库→sqlmap++） |
| N2 | **结果入库**: 开放端口/服务 → 图谱，跨会话资产复用 |

---

## 工具 4: Web 目录类（gobuster/ffuf/dirb/wfuzz — P2）

**天花板**: 纯字典暴力、无智能、不隐身、SPA 通配误报
**创新点**:
| # | 创新 |
|---|------|
| D1 | **JS 端点预提取**: 先拉前端 JS → 提取真实 API 路径 → 缩小字典（跳过纯字典） |
| D2 | **3-gate 通配过滤**: 发现的路径基线对比（SPA 全 200 自动过滤） |
| D3 | **stealth 限速**: 随机间隔 + UA 池 |

---

## 工具 5: OSINT 类（theharvester/sherlock/recon-ng — P3）

**天花板**: 单点查询、不聚合、不排序
**创新点**:
| # | 创新 |
|---|------|
| O1 | **多源聚合**: theharvester/sherlock 结果合并去重 → 优先级排序（email→username→domain） |
| O2 | **入库 → 联动**: 结果喂给爆破类社工字典生成器（C1） |

---

## 工具 6: AD/内网类（nxc/impacket/kerbrute — P3）

**天花板**: 单步工具、无攻击链编排（internal_lateral playbook 已做基础版）
**创新点**:
| # | 创新 |
|---|------|
| A1 | **攻击链编排增强**: userenum → AS-REP/Kerberoast → 密码喷洒 → 哈希传递 全自动（补 stealth 节奏） |
| A2 | **结果入库**: 有效用户/哈希 → 图谱 |

---

## 工具 7: 代码审计类（semgrep/bandit — P3）

**天花板**: 静态规则、误报高、无运行时验证
**创新点**:
| # | 创新 |
|---|------|
| E1 | **结果分级**: 规则权重 + 上下文 → 优先级排序 |
| E2 | **运行时联动**: 代码漏洞 → 关联 fastsec 扫描验证（代码层 → 运行时） |

---

## 优先级总表

| 优先级 | 工具 | 核心价值 | 依赖 |
|--------|------|---------|------|
| **P0** | sqlmap++ | 注入利用（数据泄露） | fastsec diff/stealth/3-gate |
| P1 | 爆破类 | 社工字典 + 防锁 | OSINT 结果（P3） |
| P2 | 扫描/目录 | 自动编排 | — |
| P3 | OSINT/AD/审计 | 聚合联动 | — |

## 阶段门

- **P0 门**: sqlmap++ 本地注入服务器：请求量 vs 原生 -60%、3-gate 确认注入、结果入库 candidate
- **P1 门**: 社工字典对本地爆破测试命中率提升、无锁账号
- **P2 门**: 扫描→编排→后续工具自动触发
- **P3 门**: 聚合排序 + 入库联动
