# Kali MCP Server 依赖
# Python 3.8+ required

# MCP SDK (Model Context Protocol)
mcp>=1.0.0

# 异步支持
aiohttp>=3.8.0
aiofiles>=23.0.0

# 数据验证
pydantic>=2.0.0
anthropic>=0.35.0
openai>=1.40.0

# SSE 模式依赖 (远程访问)
starlette>=0.27.0
uvicorn>=0.23.0
sse-starlette>=1.6.0

# Web 界面
flask>=2.3.0
flask-cors>=4.0.0

# 模板引擎 (报告生成)
jinja2>=3.1.0

# 可选: Word 报告导出 (export_task_report formats=docx)
python-docx>=1.0.0

# 测试
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0

# 可选: PWN工具支持
# pwntools>=4.11.0

# 可选: 浏览器自动化引擎 (反检测 + 心跳维持)
# 安装后需运行: playwright install chromium
# playwright>=1.40.0

# ---- LLM 自主多智能体：向量化知识库（P0）----
# 本地 embedding（首次运行自动下载模型到 data/models，需联网一次；之后离线可跑）
sentence-transformers>=2.7.0
# transformers 4.57.2 存在本地路径加载 bug（mistral 兼容分支对 dict 取 .model_type），
# 4.57.6 修复；sentence-transformers 拉取 transformers<5，这里钉住下限
transformers>=4.57.6,<5.0.0
# 向量存储（单文件 data/kb_vectors.db；Windows 加载失败时自动降级 numpy+JSON）
sqlite-vec>=0.1.6
# 关键词召回（混合检索 RRF 融合）
rank-bm25>=0.2.2
# 向量运算
numpy>=1.24.0

# ---- LLM 自主多智能体：联网搜索（P4，架构设计 §8）----
# 默认搜索后端（免费无 key，纯 Python；8.x 起包名迁移为 ddgs，代码两者兼容）
duckduckgo-search>=6.3.0
# 可选后端（质量更高；需 TAVILY_API_KEY，并设 WEB_SEARCH_BACKEND=tavily 切换）
# tavily-python>=0.3.0
